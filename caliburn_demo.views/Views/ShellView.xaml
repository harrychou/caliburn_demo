<Window x:Class="caliburn_demo.wpf.Views.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:pf="clr-namespace:Caliburn.PresentationFramework;assembly=Caliburn.PresentationFramework"
        Title="ShellView" Height="334" Width="560">
    <Grid x:Name="LayoutRoot"
          Background="White">
        <StackPanel>
            <TextBox Text="{Binding Name}" />
            <TextBlock Text="{Binding Message}" />
            <Button pf:Message.Attach="ChangeMessage"
                    Content="Click Me" />
        </StackPanel>
    </Grid>

</Window>
